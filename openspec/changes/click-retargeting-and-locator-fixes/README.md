# click-retargeting-and-locator-fixes

Make synthetic Click replicate AWT LightweightDispatcher retargeting so clicking a label whose listener lives on an ancestor works; fix two locator-engine bugs (capture on child chains; 3+ level CSS > chains)
